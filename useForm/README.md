# useForm

Ejemplo de uso: 
```
   const initialForm = {
      name: '',
      age: 20,
      email: 'example@gmail.com'
   }
   const [ formValues, handleInputChange, reset ] = useForm( initialForm )

```
